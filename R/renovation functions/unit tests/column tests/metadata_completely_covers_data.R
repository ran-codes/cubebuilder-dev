#' This will be tests after paritioning is compiled. TBD. will wr. goals is to amke sure all data points hvae metadata.
#'    so likely a df_data -> metadta cube that is checked against the metadata cube generated.

metadata_complelety_covers_data = function(df, i){
  
 
}